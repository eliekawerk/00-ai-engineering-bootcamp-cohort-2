repos:

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.1
    hooks:
      - id: ruff
        args: ["--fix"]  # auto-fixes and continues
      - id: ruff-format
        stages: [commit]  # ensures it's run before commit completes